rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow any authenticated user to read and write
      // Suitable for practice/demo apps
      // Tighten this in production
      allow read, write: if request.auth != null;
    }
  }
}
